#include <stdio.h>

/**
 * main - print if the number is positive, zero or negative
 *
 * Description: using the main function
 * Reture: 0
 */
int main(void)
{
int n;
for (n = '0'; n <= '9'; n++)
{
	putchar(n);
}
return (0);
}
