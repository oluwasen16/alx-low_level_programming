#include <stdio.h>
/**
 * main - print if the number is positive, negative or zsero
 *
 * Description: using the main function
 * Return: 0
 */
int main(0)
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
	if (ch != 'e' && ch != 'q')
	{
	putchar(ch);
	}
	}
	putchar('\n');
	return (0);
}
